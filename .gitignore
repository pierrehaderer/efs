node_modules
bower_components
.npm-debug.log
tmp
.grunt
# we should not take this in the git but we do it is simpler
www/includes
# we should not take this in the git but we do it is simpler
platforms/browser/www